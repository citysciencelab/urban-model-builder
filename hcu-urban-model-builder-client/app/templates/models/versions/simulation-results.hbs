{{page-title "Simulationsergebnisse"}}

<div class="simulation-results-page">
  <div class="page-header d-flex justify-content-between align-items-center mb-3">
    <div>
      <BreadcrumbsItem as |linkClass|>
        <LinkTo @route="models.versions.show" @models={{array this.model.model.id this.model.id}} class={{linkClass}}>
          {{this.model.model.internalName}}
        </LinkTo>
      </BreadcrumbsItem>
      <BreadcrumbsItem as |linkClass|>
        <LinkTo @route="models.versions.show" @models={{array this.model.model.id this.model.id}} class={{linkClass}}>
          Version {{this.model.majorVersion}}.{{this.model.minorVersion}}.{{this.model.draftVersion}}
        </LinkTo>
      </BreadcrumbsItem>
      <BreadcrumbsItem>
        Simulationsergebnisse
      </BreadcrumbsItem>
    </div>
    
    <div class="page-header-actions d-flex gap-2">
      <LinkTo @route="models.versions.show" @models={{array this.model.model.id this.model.id}} class="btn btn-outline-secondary">
        <FaIcon @icon="arrow-left" /> Zurück zum Modell
      </LinkTo>
      <button type="button" class="btn btn-primary" {{on "click" this.loadSimulationResults}}>
        <FaIcon @icon="sync-alt" /> Aktualisieren
      </button>
    </div>
  </div>

  <SimulationViewer @model={{this.model}} @reloadKey={{this.reloadKey}} />
</div>
