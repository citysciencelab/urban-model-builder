{{page-title (t "routes.models.title")}}

<BreadcrumbsItem as |linkClass|>
  <LinkTo @route="models.index" class={{linkClass}}>
    {{t "models.model.plural"}}
  </LinkTo>
</BreadcrumbsItem>

{{outlet}}