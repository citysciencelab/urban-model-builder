<BsModal
  @open={{@open}}
  @onSubmit={{this.onSubmit}}
  @onHide={{this.onHide}}
  {{did-insert this.prepare}} as |modal|
>
  <modal.header>
    <h5 class="modal-title">
      {{t "components.clone_dialog.title"}}
    </h5>
  </modal.header>
  <modal.body>
    <p>
      {{t "components.clone_dialog.intro"}}
    </p>
    <BsForm
      @formLayout="vertical"
      @model={{this.changeset}}
      @onSubmit={{this.onSubmit}} as |form|
    >
      <form.element
        @controlType="text"
        @label={{t "models.model.attributes.name"}}
        @property="internalName"
      />
    </BsForm>
  </modal.body>
  <modal.footer>
    <BsButton @onClick={{this.onHide}}>
      {{t "actions.cancel"}}
    </BsButton>
    <BsButton @type="success" @onClick={{this.onSubmit}}>
      {{t "actions.clone"}}
    </BsButton>
  </modal.footer>
</BsModal>